# データセマンティクス管理ツール設計および政府統計を用いた概念実証（PoC）計画

## 1. 目的と背景

エンタープライズ環境における最大の課題は、システム間での「言葉の意味のズレ」によるデータのサイロ化と、AIのハルシネーション（誤推論）である。本計画は、データの意味をDuckDBのメタデータ（テーブル・カラムコメント）として定義し、同名異義語（異なる意味の同じ言葉）とシノニム（同じ意味の異なる言葉）を整理して、RAGベースで検索し、質問時に安全なSQLを生成できることを実証する。

本設計では、独立した巨大なセマンティック辞書を置くよりも、DuckDB 上のテーブル・カラムコメントを事実上の意味レイヤーとして扱う。これにより、実データと意味が同じ文脈で保たれ、AIが SQL 生成時にメタデータを根拠に判断できるようにする。

---

## 2. 第1部：DuckDBメタデータRAGを主役とするセマンティクス設計

本ツールは、物理的なETLや重厚なグラフDB導入を行わず、データ仮想化基盤の一部としてDuckDBに載るメタデータ層を活用する。AIエージェントは、ユーザーの質問に対してまずメタデータを検索し、その意味情報から適切なテーブルとカラムを決め、最後にSQLを生成する。

### 2.1 テーブルとカラムへの意味コメントの設計

DuckDBの COMMENT ON TABLE と COMMENT ON COLUMN を活用し、各テーブルとカラムに定義・単位・ソースシステム・注意点を埋め込む。

#### 2.1.1 テーブルコメントの例

- `stat_census_population`
  - 「国勢調査の人口。定義は『実際に居住している人間の数』。住民票上の人数ではない。」
- `residence_registry_population`
  - 「住民基本台帳の人口。定義は『住民票に登録されている人数』。居住実態とは異なる。」
- `economic_census_workers`
  - 「経済センサスの働く人。『従業者』を表す。雇用形態や産業分類に基づく定義を持つ。」

#### 2.1.2 カラムコメントの例

| テーブル | カラム | コメントの例 |
| --- | --- | --- |
| `stat_census_population` | `population` | `実態人口。実際に居住している人間の数。source_system=stat_census; definition=actual_resident_population` |
| `residence_registry_population` | `registered_population` | `登録人口。住民票に登録されている人数。source_system=residence_registry; definition=registered_population` |
| `economic_census_workers` | `workers` | `従業者。働く人の概念。source_system=economic_census; alias=従業者` |
| `corporate_statistics_workers` | `employees` | `役員及び従業員。企業に所属する労働者。source_system=corporate_statistics; alias=役員及び従業員` |

### 2.2 メタデータRAGの流れ

人間が手作業で辞書を管理するのではなく、データ定義をコメントに置き、LLMや埋め込みモデルで検索可能な形にする。

1. **取り込み（DuckDBロード）:** e-StatのCSVをDuckDBのテーブルとして読み込み、コメントを付与する。
2. **索引化（metadata embedding）:** テーブルコメントとカラムコメントを検索可能なドキュメントとして埋め込み、ベクトルDBまたはDuckDB内インデックスに格納する。
3. **質問時の検索:** AIエージェントがユーザーから質問を受けた際、質問文と近い意味情報を取得する。
4. **SQL生成:** 取得したメタデータから、どのテーブルとカラムが対象かを決定し、SQLを生成する。
5. **ガードレール検査:** もし複数の定義が競合していれば、SQL生成前に clarification を返し、合算や誤認を防ぐ。

### 2.3 実装上の比較

独立辞書ではなくDuckDBコメントRAGを使う利点は以下の通りである。

- データ定義と実データの保存場所が同一であるため、意味のズレが起きにくい
- SQL生成時に必ず実際のテーブル名・カラム名が出てくる
- 既存のデータ仮想化基盤との親和性が高い
- 追加のETL基盤を作らずに、コメントのみで意味を管理できる

---

## 3. 第2部：政府統計（e-Stat）を用いた検証計画（PoC）

企業データの代わりに、縦割り行政によって「言葉の定義のズレ」が大量に存在する政府統計のオープンデータを活用し、ツールの「同名異義語の分離」と「シノニムの統合」能力を検証する。

### 3.1 検証シナリオ A：同名異義語（Equivocation）の検知とコード値に関するメタデータ

同じ「人口」という言葉でも、定義が異なるデータが存在することを、メタデータ検索とSQL生成の前段で検出できるかを検証する。

* **使用データ:**
* 国勢調査ベースの人口データ
  * e-Stat 統計表ID: 00200524
  * [令和７年国勢調査 速報集計 人口速報集計　（男女別人口及び世帯総数）](https://www.e-stat.go.jp/statistics/00200524)
  * [人口推計](https://www.stat.go.jp/data/jinsui/1.html#sakusei)

* 住民基本台帳ベースの人口データ
  * e-Stat 統計表ID: 00200241 系（住民基本台帳に基づく人口、人口動態及び世帯数調査）
  * [住民基本台帳に基づく人口、人口動態及び世帯数調査](https://www.e-stat.go.jp/stat-search/files?page=1&layout=normal&toukei=00200241&tstat=000001039591&metadata=1&data=1)
  * [説明資料](https://www.soumu.go.jp/main_content/000892926.pdf)

* 国勢調査ベース人口と住民基本台帳ベース人口の混在データ
  * [社会・人口統計体系](https://www.e-stat.go.jp/stat-search/database?page=1&layout=datalist&toukei=00200502&tstat=000001111375&cycle=8&tclass1=000001111377&result_page=1&tclass2val=0)
  *[説明資料](https://www.stat.go.jp/data/ssds/1.html)


* **PoCのタスク:** エージェントに「A市の人口推移を出して」と指示する。
* **成功基準（期待される挙動）:**
1. **メタデータ検索による定義認識:** `population` という語に対して、`stat_census_population.population` と `residence_registry_population.registered_population` の両方がヒットすること。
2. **合算・混同の阻止:** `definition` が異なるメタデータを持つカラムを自動的に合算しないこと。
3. **弁証法的な対話:** ユーザーに対し「実態ベースと登録ベースのどちらを指していますか？」と確認すること。
4. **差分からのインサイト導出:** 「登録人口と実態人口の差分」を計算し、「住民票はあるが住んでいない人が〇〇人いる」といった分析を行えること。

### 3.2 検証シナリオ B：シノニム（異名同義語）の正規化と透過的クエリ

異なる言葉で表現されているが、実質的に同じ意味を持つデータを、メタデータ検索から内部的に正規化し、各ソースの正しい物理カラムへ安全にマッピングできるかを検証する。

* **使用データ:**
* 『経済センサス』の「働く人」（項目名：`従業者`）
* 『法人企業統計』の「働く人」（項目名：`役員及び従業員`）

* **PoCのタスク:** エージェントに「東京都のIT産業の『働く人』の数を両方の統計から比較して」と指示する。
* **成功基準（期待される挙動）:**
1. **メタデータRAGによる正規化:** 「働く人」という曖昧な語が、各カラムコメントに含まれる `definition` や `alias` をもとに、`従業者` と `役員及び従業員` に対応づけられること。
2. **透過的SQL生成:** それぞれのテーブルに対し、適切な物理カラム名を使用した別々のSQLを生成し、結果を結合して回答できること。
3. **比較の説明可能性:** どのカラムがどの統計のどの定義を表しているかを、ユーザーに説明可能であること。

### 3.3 PoCシステム構成

ノートPC上でポータブルに完結する「Local COA」環境として構築する。

* **データストア:** DuckDB（e-StatのCSVデータを生テーブルとしてロード）
* **意味レイヤー:** DuckDBのテーブル/カラムコメント（`COMMENT ON TABLE` / `COMMENT ON COLUMN`）
* **検索層:** コメントをRAGとして埋め込み・検索するローカルベクトル検索層
* **検証レイヤー (organon-util):** Python + PySHACL / 検証ルールで、異なる定義の合算禁止や矛盾チェックを行う
* **AIエージェント:** LangGraph + LLM（OpenAI API等）によるステートマシン制御とSQL生成

---

## 4. 実装の重要な設計判断

### 4.1 セマンティック辞書の主役は別に置かない

本PoCでは、辞書テーブルを主役にせず、DuckDBメタデータを主役にする。理由は以下の通りである。

- 実データと意味の定義が同じリポジトリに存在する
- SQL生成時に、意味と物理名の対応が直列で追跡可能である
- 既存のDBに対して、そのまま意味コメントを付与できる
- 実運用でも導入しやすく、Zero-ETLアーキテクチャに近い

### 4.2 ルールの基本は「安全側に倒す」

AIが不確実な場合、次の原則を守る。

- 似ているだけの定義を足し合わせない
- メタデータが競合するなら質問を返す
- 物理カラム名が不明ならSQL生成を停止する
- 生成したSQLは常にデータ定義の説明可能性を伴う

---

## 5. 期待される成果

本PoCが成功すれば、大規模なETL基盤や重厚なグラフDB（Amazon Neptune等）を導入することなく、DuckDB上のメタデータコメントとRAG検索だけで、AIエージェントが意味のズレを検知し、適切なクエリに落とし込めることが証明される。これは、データ仮想化と生成AIを組み合わせた「Zero-ETLアーキテクチャ」の実証として極めて有効である。

---

## 6. 実装に向けた最初の決定

今回のPoCの中核は次の一文である。

「意味の真実の源は独立した辞書ではなく、DuckDB のテーブル・カラムコメントそのものであり、質問時にそのメタデータを検索してから SQL を生成する」

この設計により、同名異義語の誤混同とシノニムの誤正規化を、構造的に抑止できる。

---
